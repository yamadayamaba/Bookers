<h1>Books</h1>
<%= form_with model : @book,url:'book_path',local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
<% end %>
<%= link_to 'edit',edit_book_path(list.id) %>
<%= link_to 'Back',books_path(list.id) %>