<!DOCTYPE html>
<meta charset="UTF8">
<html lang="ja"></html>
<head>
  <title>アプリケーション制作1</title>
</head>


<body>
<h1>Books</h1>
<table>
  <thead>
      <tr>
          <th>Title</th>
          <th>body</th>
          <th colspan="3"></th>
      </tr>
  </thead>
  <tbody>
      <tr>
        <% @books.each do |book| %>
          <td><%= book.title %></td>
          <tb><%= book.body %></tb>
          <tb><%= link_to 'Show',book_path(@book) %></tb>
          <tb><%= link_to 'Edit',edit_book_path(@book) %></tb>
          <tb><%= link_to 'Destroy',book_path(@book),method: :delete, date: { confirm: "Are you sure?"} %></tb>
      <% end %>
      </tr>
  </tbody>
  </table>
  
<h2>New Book</h2>

<%= form_with model:@book, local:true do |f| %>

  <h4>title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>

<% end %>
</body>